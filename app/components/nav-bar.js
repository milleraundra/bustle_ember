import Ember from 'ember';

export default Ember.Component.extend({
  //to use transitionTo outside of a route you need to inject the router inside of every component. You can then accessit using this.get('router'). See initializers/* for the rest of the process
});
